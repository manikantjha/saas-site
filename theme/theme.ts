import { createTheme } from "@mui/material";

let theme = createTheme({
  palette: {
    primary: {
      main: "#0ea273",
      // lighter: "#f0fafb",
    },
    secondary: {
      main: "#001f3e",
    },
  },
});

export default theme;
